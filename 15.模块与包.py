# 模块（Module） 本质上就是一个以 .py 为后缀的代码文件，它可以封装变量、函数、类等代码单元，是 Python 实现代码复用、逻辑拆分和命名空间隔离的基础单元。
"""
Python 自带了丰富的标准库模块，无需额外安装即可导入使用，常用的核心模块包括：
math：基础数学计算，提供三角函数、数学常数、开方等运算能力
random：随机数生成，支持随机整数、随机抽样、序列打乱等
os：操作系统交互，处理文件路径、目录增删、环境变量等
sys：解释器交互，获取命令行参数、修改模块搜索路径、退出程序
time / datetime：时间与日期处理，支持时间戳、日期格式化、时间差计算
json：JSON 数据序列化与解析，是数据交互的核心工具
re：正则表达式，用于字符串的匹配、查找、替换
collections：扩展数据结构，包含计数器、双端队列、命名元组等
"""

'''
#1.1 安装模块（最主流的工具是 pip，太多的就不在这说了）
#python -m pip install git+https://github.com/用户名/仓库名.git   （直接从 GitHub 等仓库安装）

#1.2 自定义模块
#1.2.1 创建自定义模块（新建一个 .py 文件并写入代码，该文件就成为一个可导入的模块）
my_module = "初次尝试"
def first(a, b)
    return a + b

#1.3 模块导入与使用（正常的话开始在另一个文件操作）
#1.3.1 完整导入模块
import 模块名

#1.3.2 别名导入
import 模块名 as 别名

#1.3.3 导入模块中指定成员
from 模块名 import 成员1, 成员2

#1.3.4 导入模块所有公开成员
from 模块名 import *

#1.4 __name__（内置变量） 特殊属性
当模块被直接运行时，__name__ 的值为字符串 '__main__'
当模块被其他文件导入时，__name__ 的值为模块的文件名
# my_module.py
def add(a, b):
    return a + b
# 仅当直接运行此文件时，才执行下面的测试逻辑
if __name__ == '__main__':
    print(add(1, 2))  # 模块自测用例

# 包（Package）：模块的层级管理（标准的 Python 包本质是一个包含 __init__.py 文件的文件夹）
'''
# import 包.模块	                    命名清晰，无命名冲突	    模块函数多，防止重名
# from 包 import 模块	            写法简短	                需要频繁使用该模块
# from 包.模块 import 函数	        调用最简洁	            只用到少数几个函数（推荐）
# __init__预导入+from 包 import 函数	对外隐藏内部结构	        封装第三方工具包